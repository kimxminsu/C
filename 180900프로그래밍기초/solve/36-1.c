#include <stdio.h>
int main () {
	int x,r,y,money,i,year;
	scanf("%d %d %d", &x, &r, &y);
	year=0;
	for (i=0;money>0;i++); {
		money=x-y;
		money=money+(money*0.08);
		year++;
		printf("%d\n", money);
	}
	printf("%d", year);
}
