#include <stdio.h>
#include <stdio.h>
int big (int number) {
	int temp1=-1000001;
	if (number > temp1) temp1 = number;
	return temp;
}
int main () {
	int n;
	while (n=getchar()!='\n'){
		int n,temp,result;
		scanf("%d", &n);
		result=big(n);
	}
	printf("%d", result);
}
